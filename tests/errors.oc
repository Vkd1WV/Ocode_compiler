# Declaration errors
byte8  const good

undefined_symbol

word asm not_a_function

word const a_constant:=good:=5

fun function:=bad_init

word uninitialized

# this line causes a segfault in yylex()
#__bad_name := uninitialized

